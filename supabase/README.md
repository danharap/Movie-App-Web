# Supabase migrations

Apply `migrations/*.sql` to your Supabase project (SQL editor or `supabase db push` with the Supabase CLI linked to the project).

After applying, confirm **Auth → Providers** (email) and **Auth → URL configuration** match your deployed app.
