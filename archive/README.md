# Archive

## `backend-fastapi/`

The previous **FastAPI + SQLAlchemy + JWT** API is kept here for reference only. The production app uses **Next.js + Supabase + TMDb** instead. Do not run both stacks against production data.
