/** Product name and keys — single source of truth for branding. */
export const APP_NAME = "Nudge Film";
export const APP_NAME_SLUG = "nudgefilm";

/** sessionStorage key for last recommendation run */
export const STORAGE_KEY_LAST_RECOMMENDATION = `${APP_NAME_SLUG}:lastRecommendation`;
